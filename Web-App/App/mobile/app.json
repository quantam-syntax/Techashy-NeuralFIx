{
  "expo": {
    "name": "NetFixAI",
    "slug": "netfixai",
    "version": "1.0.0",
    "orientation": "portrait",
    "userInterfaceStyle": "dark",
    "splash": { "backgroundColor": "#0A0C0F" },
    "ios": {
      "bundleIdentifier": "com.netfixai.app",
      "infoPlist": {
        "NSCameraUsageDescription": "NetFixAI needs camera access to photograph networking equipment.",
        "NSPhotoLibraryUsageDescription": "NetFixAI needs photo access to upload equipment images."
      }
    },
    "android": {
      "package": "com.netfixai.app",
      "permissions": ["CAMERA", "READ_EXTERNAL_STORAGE"]
    },
    "plugins": [
      ["expo-image-picker", {
        "photosPermission": "Allow NetFixAI to access your photos.",
        "cameraPermission": "Allow NetFixAI to use the camera."
      }]
    ]
  }
}
